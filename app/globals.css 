@import url("https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;800&family=Rajdhani:wght@400;500;600;700&display=swap");

:root {
  --bp-half-mobile: 320px;
  --bp-mobile: 480px;
  --bp-tablet: 768px;
  --bp-laptop: 1024px;
  --bp-desktop: 1280px;
  --bp-tv: 1536px;
  --bp-big-screen: 1920px;
  --bp-projector: 2560px;

  --bg: #050816;
  --bg-2: #09101f;
  --bg-3: #0d1630;
  --panel: rgba(10, 16, 32, 0.7);
  --panel-2: rgba(14, 23, 46, 0.82);
  --panel-3: rgba(18, 30, 58, 0.92);

  --line: rgba(115, 170, 255, 0.18);
  --line-strong: rgba(115, 170, 255, 0.34);

  --text: #d8e7ff;
  --text-soft: #94a6c8;
  --text-dim: #66789b;

  --cyan: #55f6ff;
  --pink: #ff4fd8;
  --violet: #7b61ff;
  --blue: #5ea8ff;
  --green: #53ffa8;
  --gold: #ffc857;

  --shadow-deep:
    0 12px 40px rgba(0, 0, 0, 0.45),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);

  --glow-cyan:
    0 0 0 1px rgba(85, 246, 255, 0.28),
    0 0 24px rgba(85, 246, 255, 0.16);

  --glow-pink:
    0 0 0 1px rgba(255, 79, 216, 0.22),
    0 0 28px rgba(255, 79, 216, 0.16);

  --radius-sm: 14px;
  --radius-md: 20px;
  --radius-lg: 28px;
  --radius-xl: 36px;

  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-5: 1.25rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-10: 2.5rem;
  --space-12: 3rem;
  --space-16: 4rem;
  --space-20: 5rem;

  --text-xs: clamp(0.76rem, 0.73rem + 0.15vw, 0.84rem);
  --text-sm: clamp(0.9rem, 0.84rem + 0.2vw, 1rem);
  --text-base: clamp(1rem, 0.95rem + 0.25vw, 1.12rem);
  --text-lg: clamp(1.12rem, 1rem + 0.55vw, 1.45rem);
  --text-xl: clamp(1.4rem, 1.1rem + 1.2vw, 2.25rem);
  --text-2xl: clamp(2rem, 1.4rem + 2.3vw, 4rem);
  --text-hero: clamp(2.5rem, 1.4rem + 4vw, 6rem);

  --max-shell: min(1600px, calc(100vw - 32px));
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  color-scheme: dark;
  -webkit-text-size-adjust: 100%;
  scroll-behavior: smooth;
}

body {
  margin: 0;
  min-height: 100dvh;
  background:
    radial-gradient(circle at top left, rgba(85, 246, 255, 0.08), transparent 22%),
    radial-gradient(circle at top right, rgba(255, 79, 216, 0.08), transparent 20%),
    linear-gradient(180deg, var(--bg), var(--bg-2) 45%, var(--bg-3));
  color: var(--text);
  font-family: "Rajdhani", sans-serif;
  font-size: var(--text-base);
  line-height: 1.55;
  overflow-x: hidden;
}

a {
  color: inherit;
  text-decoration: none;
}

button,
input,
textarea,
select {
  font: inherit;
}

img,
svg,
canvas {
  display: block;
  max-width: 100%;
}

::selection {
  background: rgba(85, 246, 255, 0.24);
  color: white;
}

.cyber-app-frame {
  position: relative;
  min-height: 100dvh;
  isolation: isolate;
}

.cyber-app-frame::before {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;
  background:
    radial-gradient(circle at 15% 20%, rgba(85, 246, 255, 0.08), transparent 18%),
    radial-gradient(circle at 85% 12%, rgba(255, 79, 216, 0.08), transparent 16%),
    radial-gradient(circle at 50% 85%, rgba(123, 97, 255, 0.1), transparent 22%);
  z-index: -2;
}

.cyber-app-frame::after {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;
  background:
    repeating-linear-gradient(
      to bottom,
      rgba(255, 255, 255, 0) 0px,
      rgba(255, 255, 255, 0) 2px,
      rgba(85, 246, 255, 0.035) 3px,
      rgba(85, 246, 255, 0.015) 4px
    );
  opacity: 0.45;
  mix-blend-mode: screen;
  z-index: -1;
}

.cyber-skip-link {
  position: absolute;
  left: 12px;
  top: -60px;
  z-index: 999;
  padding: 10px 14px;
  border-radius: 12px;
  background: #0b1020;
  color: white;
  border: 1px solid rgba(85, 246, 255, 0.28);
}

.cyber-skip-link:focus {
  top: 12px;
}

.cyber-shell {
  width: var(--max-shell);
  margin-inline: auto;
  padding: clamp(14px, 2vw, 28px);
}

.cyber-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(14px, 1.6vw, 28px);
}

.panel-row {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(12px, 1.5vw, 24px);
}

.cyber-panel,
.cyber-card,
.cyber-stat,
.cyber-chart,
.cyber-hero-card {
  position: relative;
  overflow: hidden;
  border: 1px solid var(--line);
  border-radius: var(--radius-md);
  background:
    linear-gradient(180deg, rgba(255, 255, 255, 0.04), rgba(255, 255, 255, 0.01)),
    var(--panel);
  backdrop-filter: blur(18px) saturate(150%);
  box-shadow: var(--shadow-deep);
}

.cyber-panel::before,
.cyber-card::before,
.cyber-stat::before,
.cyber-chart::before,
.cyber-hero-card::before {
  content: "";
  position: absolute;
  inset: 0;
  background:
    linear-gradient(130deg, rgba(85, 246, 255, 0.08), transparent 34%, transparent 68%, rgba(255, 79, 216, 0.08));
  pointer-events: none;
}

.cyber-panel-inner,
.cyber-card-inner,
.cyber-stat-inner,
.cyber-chart-inner,
.cyber-hero-inner {
  position: relative;
  z-index: 1;
  padding: clamp(16px, 2vw, 28px);
}

.cyber-topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: clamp(16px, 2vw, 24px);
}

.cyber-brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.cyber-logo {
  width: 44px;
  height: 44px;
  border-radius: 14px;
  background:
    linear-gradient(135deg, rgba(85, 246, 255, 0.18), rgba(123, 97, 255, 0.12));
  border: 1px solid rgba(85, 246, 255, 0.24);
  box-shadow: var(--glow-cyan);
  position: relative;
  flex: 0 0 auto;
}

.cyber-logo::before,
.cyber-logo::after {
  content: "";
  position: absolute;
  inset: 10px;
  border: 1px solid rgba(85, 246, 255, 0.45);
  clip-path: polygon(18% 0, 100% 0, 100% 82%, 82% 100%, 0 100%, 0 18%);
}

.cyber-logo::after {
  inset: 15px;
  border-color: rgba(255, 79, 216, 0.4);
}

.cyber-kicker,
.cyber-label,
.cyber-mini-label {
  font-family: "Orbitron", sans-serif;
  text-transform: uppercase;
  letter-spacing: 0.18em;
}

.cyber-kicker {
  font-size: var(--text-xs);
  color: var(--cyan);
}

.cyber-title {
  margin: 0;
  font-family: "Orbitron", sans-serif;
  font-size: var(--text-xl);
  line-height: 1.1;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.cyber-hero-title {
  margin: 0;
  font-family: "Orbitron", sans-serif;
  font-size: var(--text-hero);
  line-height: 0.95;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  text-shadow:
    0 0 14px rgba(85, 246, 255, 0.22),
    0 0 32px rgba(123, 97, 255, 0.16);
}

.cyber-subtitle {
  color: var(--text-soft);
  font-size: var(--text-base);
  max-width: 65ch;
}

.cyber-pill-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 18px;
}

.cyber-pill {
  display: inline-flex;
  align-items: center;
  min-height: 42px;
  padding: 10px 14px;
  border-radius: 999px;
  border: 1px solid rgba(85, 246, 255, 0.2);
  background: rgba(85, 246, 255, 0.08);
  color: var(--text);
  font-size: var(--text-sm);
}

.cyber-button,
.cyber-button-secondary {
  min-height: 48px;
  padding: 12px 16px;
  border-radius: 16px;
  border: 1px solid transparent;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  cursor: pointer;
  transition: transform 180ms ease, box-shadow 180ms ease, border-color 180ms ease, background 180ms ease;
}

.cyber-button {
  color: #04111a;
  background: linear-gradient(135deg, var(--cyan), #8dfcff);
  box-shadow: var(--glow-cyan);
  font-weight: 700;
}

.cyber-button-secondary {
  color: var(--text);
  background: rgba(255, 255, 255, 0.04);
  border-color: rgba(255, 255, 255, 0.08);
}

.cyber-button:hover,
.cyber-button-secondary:hover {
  transform: translateY(-2px);
}

.cyber-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.cyber-textarea {
  width: 100%;
  min-height: 180px;
  resize: vertical;
  border-radius: 22px;
  border: 1px solid var(--line);
  background:
    linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.01)),
    rgba(7, 12, 26, 0.92);
  color: var(--text);
  padding: 18px;
  outline: none;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.04);
}

.cyber-textarea:focus {
  border-color: rgba(85, 246, 255, 0.46);
  box-shadow: var(--glow-cyan);
}

.cyber-stat-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(12px, 1.3vw, 20px);
}

.cyber-stat {
  container-type: inline-size;
}

.cyber-stat-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
}

.cyber-stat-title {
  color: var(--text-soft);
  font-size: var(--text-sm);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.cyber-stat-value {
  margin-top: 8px;
  font-family: "Orbitron", sans-serif;
  font-size: clamp(1.5rem, 1.1rem + 1.5cqw, 2.8rem);
  line-height: 1;
  letter-spacing: 0.05em;
}

.cyber-stat-trend {
  font-size: var(--text-sm);
  color: var(--green);
  white-space: nowrap;
}

.cyber-chart-bars {
  display: grid;
  gap: 14px;
  margin-top: 14px;
}

.cyber-bar-row {
  display: grid;
  gap: 8px;
}

.cyber-bar-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  color: var(--text-soft);
  font-size: var(--text-sm);
}

.cyber-bar-track {
  width: 100%;
  height: 12px;
  border-radius: 999px;
  background: rgba(255,255,255,0.06);
  overflow: hidden;
  border: 1px solid rgba(255,255,255,0.06);
}

.cyber-bar-fill {
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--cyan), var(--pink));
  box-shadow: 0 0 18px rgba(85, 246, 255, 0.3);
}

.cyber-score-ring {
  width: clamp(120px, 22vw, 180px);
  aspect-ratio: 1 / 1;
  border-radius: 50%;
  display: grid;
  place-items: center;
  margin-inline: auto;
  background:
    radial-gradient(circle at center, rgba(5,8,22,0.95) 0 53%, transparent 54%),
    conic-gradient(var(--cyan) 0 72%, rgba(255,255,255,0.08) 72% 100%);
  box-shadow: var(--glow-cyan);
  position: relative;
}

.cyber-score-ring::after {
  content: "";
  position: absolute;
  inset: 10px;
  border-radius: 50%;
  border: 1px solid rgba(85, 246, 255, 0.2);
}

.cyber-score-value {
  position: relative;
  z-index: 1;
  text-align: center;
  font-family: "Orbitron", sans-serif;
  font-size: clamp(1.6rem, 1rem + 1.6vw, 2.8rem);
  line-height: 1;
}

.cyber-score-caption {
  display: block;
  margin-top: 6px;
  color: var(--text-soft);
  font-size: var(--text-xs);
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.cyber-list {
  display: grid;
  gap: 12px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.cyber-list-item {
  display: flex;
  gap: 12px;
  align-items: flex-start;
  padding: 12px 14px;
  border-radius: 16px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.06);
}

.cyber-dot {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  margin-top: 8px;
  background: var(--cyan);
  box-shadow: 0 0 14px rgba(85, 246, 255, 0.5);
  flex: 0 0 auto;
}

.cyber-side-stack {
  display: grid;
  gap: clamp(12px, 1.4vw, 20px);
}

.cyber-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  min-height: 36px;
  padding: 8px 12px;
  border-radius: 999px;
  background: rgba(255, 79, 216, 0.08);
  border: 1px solid rgba(255, 79, 216, 0.2);
  color: var(--text);
  font-size: var(--text-xs);
  letter-spacing: 0.14em;
  text-transform: uppercase;
}

.cyber-section-title {
  margin: 0 0 10px;
  font-family: "Orbitron", sans-serif;
  font-size: var(--text-lg);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.cyber-muted {
  color: var(--text-soft);
}

.cyber-divider {
  width: 100%;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(85, 246, 255, 0.28), transparent);
  margin: 16px 0;
}

.cyber-two-col,
.cyber-three-col,
.cyber-four-col {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(12px, 1.4vw, 22px);
}

.cyber-tilt {
  transform-style: preserve-3d;
  will-change: transform;
}

.cyber-tilt-layer {
  transform: translateZ(26px);
}

.cyber-holo-line {
  position: absolute;
  inset: auto 0 14px 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(85, 246, 255, 0.8), transparent);
  box-shadow: 0 0 18px rgba(85, 246, 255, 0.35);
  pointer-events: none;
  z-index: 2;
}

.cyber-corner-cut::after {
  content: "";
  position: absolute;
  top: 0;
  right: 0;
  width: 56px;
  height: 56px;
  background: linear-gradient(135deg, transparent 48%, rgba(85, 246, 255, 0.16) 49%, rgba(85, 246, 255, 0.06) 100%);
  clip-path: polygon(100% 0, 0 0, 100% 100%);
  pointer-events: none;
}

.cyber-grid-lines {
  position: absolute;
  inset: 0;
  pointer-events: none;
  background-image:
    linear-gradient(rgba(94, 168, 255, 0.08) 1px, transparent 1px),
    linear-gradient(90deg, rgba(94, 168, 255, 0.08) 1px, transparent 1px);
  background-size: 26px 26px;
  mask-image: radial-gradient(circle at center, black 34%, transparent 90%);
  opacity: 0.25;
  z-index: 0;
}

.cyber-noise {
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: 0.05;
  background-image:
    radial-gradient(rgba(255,255,255,0.5) 0.6px, transparent 0.8px);
  background-size: 8px 8px;
  mix-blend-mode: soft-light;
  z-index: 1;
}

@container (min-width: 430px) {
  .cyber-stat-head {
    align-items: center;
  }
}

@media (min-width: 320px) {
  .cyber-shell {
    width: min(100%, calc(100vw - 16px));
    padding: 10px;
  }

  .cyber-topbar {
    flex-direction: column;
    align-items: stretch;
  }

  .cyber-actions {
    flex-direction: column;
  }

  .cyber-button,
  .cyber-button-secondary {
    width: 100%;
  }
}

@media (min-width: 480px) {
  .cyber-shell {
    width: min(100%, calc(100vw - 24px));
    padding: 12px;
  }

  .cyber-actions {
    flex-direction: row;
  }

  .cyber-button,
  .cyber-button-secondary {
    width: auto;
  }

  .cyber-two-col {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (min-width: 768px) {
  .cyber-topbar {
    flex-direction: row;
    align-items: center;
  }

  .cyber-grid {
    grid-template-columns: 1fr;
  }

  .panel-row.split-2 {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .cyber-stat-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .cyber-three-col {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (min-width: 1024px) {
  .cyber-grid {
    grid-template-columns: minmax(0, 1.4fr) minmax(320px, 0.8fr);
    align-items: start;
  }

  .panel-row.split-3 {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .cyber-four-col {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (min-width: 1280px) {
  .cyber-shell {
    width: min(100%, calc(100vw - 40px), 1500px);
    padding: 18px;
  }

  .cyber-grid {
    grid-template-columns: minmax(280px, 0.9fr) minmax(0, 1.5fr) minmax(320px, 0.9fr);
  }

  .cyber-stat-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .cyber-three-col {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .cyber-four-col {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
}

@media (min-width: 1536px) {
  .cyber-shell {
    width: min(100%, calc(100vw - 56px), 1720px);
    padding: 24px;
  }

  .cyber-hero-inner,
  .cyber-card-inner,
  .cyber-panel-inner,
  .cyber-stat-inner,
  .cyber-chart-inner {
    padding: 24px;
  }
}

@media (min-width: 1920px) {
  .cyber-shell {
    width: min(100%, calc(100vw - 72px), 1880px);
  }

  .cyber-grid {
    grid-template-columns: minmax(320px, 0.85fr) minmax(0, 1.7fr) minmax(360px, 0.95fr);
  }

  .cyber-title {
    font-size: clamp(1.7rem, 1.1rem + 1vw, 2.7rem);
  }
}

@media (min-width: 2560px) {
  .cyber-shell {
    width: min(100%, calc(100vw - 100px), 2280px);
    padding: 30px;
  }

  .cyber-grid {
    grid-template-columns: minmax(340px, 0.8fr) minmax(0, 1.8fr) minmax(400px, 0.9fr);
    gap: 28px;
  }

  .cyber-hero-title {
    font-size: clamp(4rem, 2rem + 3vw, 7rem);
  }

  .cyber-card-inner,
  .cyber-panel-inner,
  .cyber-stat-inner,
  .cyber-chart-inner,
  .cyber-hero-inner {
    padding: 28px;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }

  .cyber-app-frame::after {
    opacity: 0.18;
  }

  .cyber-tilt {
    transform: none !important;
  }
}

@media (prefers-reduced-motion: no-preference) {
  .cyber-panel,
  .cyber-card,
  .cyber-stat,
  .cyber-chart,
  .cyber-hero-card,
  .cyber-button,
  .cyber-button-secondary {
    transition:
      transform 220ms cubic-bezier(0.16, 1, 0.3, 1),
      box-shadow 220ms cubic-bezier(0.16, 1, 0.3, 1),
      border-color 220ms cubic-bezier(0.16, 1, 0.3, 1),
      filter 220ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .cyber-panel:hover,
  .cyber-card:hover,
  .cyber-stat:hover,
  .cyber-chart:hover,
  .cyber-hero-card:hover {
    border-color: rgba(85, 246, 255, 0.35);
    filter: saturate(1.08);
  }

  .cyber-app-frame::after {
    animation: scan-drift 10s linear infinite;
  }

  .cyber-hero-title {
    animation: neon-breathe 4.5s ease-in-out infinite;
  }
}

@keyframes scan-drift {
  from {
    transform: translateY(0);
  }
  to {
    transform: translateY(6px);
  }
}

@keyframes neon-breathe {
  0%,
  100% {
    text-shadow:
      0 0 12px rgba(85, 246, 255, 0.16),
      0 0 28px rgba(123, 97, 255, 0.12);
  }
  50% {
    text-shadow:
      0 0 18px rgba(85, 246, 255, 0.28),
      0 0 36px rgba(255, 79, 216, 0.18);
  }
}
